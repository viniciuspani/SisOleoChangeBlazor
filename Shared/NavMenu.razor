<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">SisOleoChangeBlazor</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="oi oi-home" aria-hidden="true"></span> Index
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="filtro">
                <span class="oi oi-plus" aria-hidden="true"></span> Filtros
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="oleo">
                <span class="oi oi-list-rich" aria-hidden="true"></span> Oleos
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="veiculos">
                <span class="oi oi-document" aria-hidden="true"></span> Veiculos
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="trocaoleo">
                <span class="oi oi-plus" aria-hidden="true"></span> Troca Oleo
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
